# Concurrency patterns

Core concurrency patterns implementation

1. BlockingQueue
2. Producer Consumer
3. Worker pool

## Build and run
```
mvn clean install

java -jar target/ConcurrencyPatterns-1.0-SNAPSHOT.jar <command>
```
Use help command to see all available commands
